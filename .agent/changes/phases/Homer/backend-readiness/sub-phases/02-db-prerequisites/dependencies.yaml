phase: 02-db-prerequisites
created: 2026-06-26
schema: athena-default
feature: alexandria-backend-readiness

dependencies:
  read:
    - path: docs/updated_db_fields.sql
      reason: Live Supabase schema snapshot.
      priority: critical
    - path: docs/database-engineer-reference.md
      reason: DB engineer handoff and schema polishing notes.
      priority: critical
    - path: docs/api-contracts.md
      reason: Service behavior depends on ownership and file constraints.
      priority: high
    - path: docs/backend-readiness-plan.md
      reason: Backend dependencies and schema gaps.
      priority: high
  modify: []
  create: []

relationships:
  requires_phases:
    - 01-contract-freeze
  provides_for_phases:
    - 04-auth-role-guards
    - 06-submission-review-file-services
  blocks_if_incomplete:
    - 06-submission-review-file-services

context_estimate:
  read_tokens: 6500
  total_budget: 8500
  warning_threshold: 7000
  status: ok
  recommendation: Shane owns this by default; Homer may execute SQL if necessary.

